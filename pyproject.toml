[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trace-public-service"
version = "0.1.0"
description = "A reproducible, constraint-aware retrieval engine for public-service directories."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "TRACE contributors" }]
dependencies = []

[project.optional-dependencies]
ui = ["gradio>=5,<7"]

[project.scripts]
trace-service = "trace_engine.cli:main"
trace-pantry = "trace_engine.cli:main"
trace-chat = "trace_engine.gradio_app:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
